# LanguagesApiSpringbootTestsHeroku

How to Deploy into Heroku

```
heroku login

heroku git:clone -a aluralanguagesapi 

cd aluralanguagesapi

git status

git add .

git commit -m "<MESSAGE>"

git push heroku master
```
